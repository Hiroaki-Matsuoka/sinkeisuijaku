自分で考えてコードを書くことが大切


1.複数のクラス属性から要素を取得する所でつまづいた。
　getElementByClassNameでは HTMLCollectioが返されるため
　for (let i = 0; i <= card.length - 1; i++)のように
　複数のクラス属性に対してはその数だけ処理を行う必要がある。

2.スタートボタンを押すまでクリックしても反応しないようにする。
　cardクラスを持つ要素がクリックにより反応するので初期設定では排除した。
　代わりにスタートボタンを押すことでcard属性を付けるようにするため
　preクラスを設置して1.と同じようにfor文を使いボタンを押すとpreクラスの要素全部に
　cardクラスがつくようにした。
　
　その際にスタートボタンをクリックするイベントリスナーの中に
　cardをクリックするとひっくり返す処理を入れることで上手く処理を動かすことができた。

3.カードを返す時はsetTimeoutで時間差表示しなくては回転と同時に絵柄が
　出てしまう。

4.乱数を利用してスタートボタンを押すことでそれぞれ配置されるようにする。
　重複しない乱数をどうコーディングするか？

// .cardの数は０から始まる為lengthを−１する。
  //　０からスタートして５になるまでが６つなので６になるまで１づつ加算
  //　演算子の書き方でエラーでたので注意！ex.<= を　=<でエラー
  // アロー関数にしたらエラー出るのは何故か？


  スタートしたら画像をランダム配置できるようにする。

  5.エラーの原因はスペルミスがあった。

